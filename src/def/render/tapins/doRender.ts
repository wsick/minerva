module minerva.def.render.tapins {
    export var doRender: IRenderTapin = function (assets: IAssets, state: IState, output: IOutput, ctx: RenderContext, region: Rect): boolean {
        return true;
    };
}