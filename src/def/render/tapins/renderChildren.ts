module minerva.def.render.tapins {
    export var renderChildren: IRenderTapin = function (assets: IRenderAssets, state: IRenderState, output: IRenderOutput, ctx: CanvasRenderingContext2D, region: Rect): boolean {

        return true;
    };
}