module minerva.def.measure.tapins {
    export var invalidateFuture: IMeasureTapin = function (assets: IAssets, state: IState, output: IOutput, availableSize: Size): boolean {
        console.warn("Implement measure.tapins.invalidateFuture");
        //this.InvalidateArrange();
        //this.UpdateBounds();
        return true;
    };
}