module minerva.def.measure.tapins {
    export var applyTemplate: IMeasureTapin = function (assets: IAssets, state: IState, output: IOutput, availableSize: Size): boolean {
        return true;
    };
}