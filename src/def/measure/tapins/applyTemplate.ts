module minerva.def.measure.tapins {
    export var applyTemplate: IMeasureTapin = function (input: IInput, state: IState, output: IOutput, availableSize: Size): boolean {
        return true;
    };
}