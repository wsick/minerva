module minerva {
    export class LayoutUpdater {
        private $$pipes: IPipe[] = [];

        render() {
            var pipedef = new render.RenderPipe(); //TODO: remove

        }
    }
}