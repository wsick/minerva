module minerva.layout.measure.tapins {
    export var applyTemplate: IMeasureTapin = function (input: IInput, state: IState, output: IOutput, tree: layout.IUpdaterTree, availableSize: Size): boolean {
        return true;
    };
}